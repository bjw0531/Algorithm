#include <iostream>
#include <algorithm>
using namespace std;
using LL = long long;

LL a,b,c,d;
LL result;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    while(1)
    {
        cin >> a >> b;
        if(!a&&!b)  break;
        cout << a+b << endl;

    }
}
