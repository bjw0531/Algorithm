#include <iostream>
#include <string>
#include <stdio.h>
#include <algorithm>
#include <map>
using namespace std;

map<char,int> m;

int main()
{
	string a;
	while(getline(cin, a))
	{
		cout << a << endl;
	}
}